<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/> <!-- Warna background -->
    <corners android:radius="16dp"/> <!-- Radius sudut -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp"/>
    <stroke
        android:width="1dp"
        android:color="@android:color/darker_gray"/> <!-- Garis tepi, bisa dihapus jika tidak diinginkan -->
</shape>